source('~/Desktop/TravelScraper/Visualise/Clustering.R')
View(hotels)
source('~/Desktop/TravelScraper/Visualise/Clustering.R')
source('~/Desktop/TravelScraper/Visualise/Clustering.R')
source('~/Desktop/TravelScraper/Visualise/Clustering.R')
source('~/Desktop/TravelScraper/Visualise/Clustering.R')
library(factoextra)
hotels <- read.csv("VizData_Singapore.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,2] <- NULL #Removing the first column
png(file = "SingaporeClusters.png")
km <- kmeans(hotels, centers = 2)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
library(readr)
VizData_Bangkok <- read_csv("VizData_Bangkok.csv")
View(VizData_Bangkok)
library(readr)
VizData_Bangkok <- read_csv("VizData_Bangkok.csv",
col_names = FALSE)
View(VizData_Bangkok)
library(readr)
VizData_KualaLumpur <- read_csv("VizData_KualaLumpur.csv",
col_names = FALSE)
View(VizData_KualaLumpur)
library(readr)
VizData_Singapore <- read_csv("VizData_Singapore.csv",
col_names = FALSE)
View(VizData_Singapore)
library(factoextra)
hotels <- read.csv("VizData_KualaLumpur.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,1] <- NULL #Removing the first column
png(file = "KLClusters.png")
km <- kmeans(hotels, centers = 2)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
library(factoextra)
hotels <- read.csv("VizData_KualaLumpur.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,1] <- NULL #Removing the first column
png(file = "KLClusters.png")
km <- kmeans(hotels, centers = 2)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
library(factoextra)
hotels <- read.csv("VizData_KualaLumpur.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,1] <- NULL #Removing the first column
png(file = "KLClusters2.png")
km <- kmeans(hotels, centers = 3)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
library(factoextra)
hotels <- read.csv("VizData_KualaLumpur.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,3] <- NULL #Removing the first column
png(file = "KLClusters3.png")
km <- kmeans(hotels, centers = 3)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
library(factoextra)
hotels <- read.csv("VizData_KualaLumpur.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,3] <- NULL #Removing the first column
png(file = "KLClusters.png")
km <- kmeans(hotels, centers = 4)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
library(factoextra)
hotels <- read.csv("VizData_KualaLumpur.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,3] <- NULL #Removing the first column
hotels[,0] <- NULL #Removing the first column
png(file = "KLClusters4.png")
km <- kmeans(hotels, centers = 3)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
km
km.cluster
km["centers"]
library(factoextra)
hotels <- read.csv("VizData_KualaLumpur.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,2] <- NULL #Removing the first column
png(file = "KLClusters4.png")
km <- kmeans(hotels, centers = 3)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
km["centers"]
View(VizData_KualaLumpur)
View(VizData_KualaLumpur)
source('~/Desktop/TravelScraper/Visualise/Clustering.R')
library(factoextra)
hotels <- read.csv("VizData_KualaLumpur.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
#hotels[,2] <- NULL #Removing the first column
png(file = "KLClusters4.png")
km <- kmeans(hotels, centers = 3)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
library(factoextra)
hotels <- read.csv("VizData_KualaLumpur.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,3] <- NULL #Removing the first column
png(file = "KLClusters4.png")
km <- kmeans(hotels, centers = 3)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
library(factoextra)
hotels <- read.csv("VizData_KualaLumpur.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,3] <- NULL #Removing the first column
png(file = "KLClusters5.png")
km <- kmeans(hotels, centers = 4)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
library(factoextra)
hotels <- read.csv("VizData_KualaLumpur.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,2] <- NULL #Removing the first column
png(file = "KLClusters4.png")
km <- kmeans(hotels, centers = 3)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
library(factoextra)
hotels <- read.csv("VizData_Singapore.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,2] <- NULL #Removing the first column
png(file = "SGClusters4.png")
km <- kmeans(hotels, centers = 2)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
km["centers"]
library(factoextra)
hotels <- read.csv("VizData_Bangkok.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,2] <- NULL #Removing the first column
png(file = "BKClusters4.png")
km <- kmeans(hotels, centers = 3)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
library(factoextra)
hotels <- read.csv("VizData_Bangkok.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,2] <- NULL #Removing the first column
png(file = "BKClusters4.png")
km <- kmeans(hotels, centers = 3)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
km["clusters"]
km["centers"]
km["center"]
km
library(factoextra)
hotels <- read.csv("VizData_Singapore.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,2] <- NULL #Removing the first column
png(file = "SGClusters4.png")
km <- kmeans(hotels, centers = 2)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
km
library(factoextra)
hotels <- read.csv("VizData_KualaLumpur.csv")
rownames(hotels) <- hotels[,1] #Assigning row names from 1st column
hotels[,1] <- NULL #Removing the first column
hotels[,2] <- NULL #Removing the first column
png(file = "KLClusters4.png")
km <- kmeans(hotels, centers = 4)
# Visualize the clusters
fviz_cluster(km, data = hotels)
# saving the file
dev.off()
km
